kind: benchmark
id: ovtest-memory
version: 0.1.0
entry:
  case_builder: scripts/build_tasks.py
dataset:
  default_split: smoke
execution:
  mode: multi_turn
  requires_stateful_agent: false
  task_isolation: per_task
judging:
  mode: builtin
